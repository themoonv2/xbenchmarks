---
import Layout from "../layouts/Layout.astro";
---

<Layout title="XBenchmarks - Hardware Database">
	<section class="hero">
		<h1 aria-label="XBenchmarks">XBenchmarks</h1>
		<p class="intro" aria-label="XBenchmarks Introduction">
			<strong>XBenchmarks</strong> is a comprehensive hardware benchmarking
			and comparison platform designed to help users evaluate laptops, CPUs,
			and GPUs using real-world performance data.<br /><br />

			XBenchmarks combines detailed technical specifications with
			standardized benchmarks to turn complex metrics into clear,
			actionable insights.<br /><br />

			The sections below focus on complete laptop systems, individual
			processors, and graphics solutions, helping users understand
			performance differences and make confident hardware decisions.
		</p>

		<div
			class="grid"
			style="grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); margin-top: var(--space-6);"
		>
			<a
				href="/laptop-brands"
				class="card"
				aria-label="Laptop Brands"
			>
				<h2 aria-label="Laptops">Laptops</h2>
				<p aria-label="Laptops Description">
					Browse detailed specs and configurations by Brand.
				</p>
				<span class="arrow">&rarr;</span>
			</a>
			<a
				href="/cpus"
				class="card"
				aria-label="CPUs"
			>
				<h2 aria-label="CPUs">CPUs</h2>
				<p aria-label="CPUs Description">
					Processor benchmarks and technical comparisons.
				</p>
				<span class="arrow">&rarr;</span>
			</a>
			<a
				href="/gpus"
				class="card"
				aria-label="GPUs"
			>
				<h2 aria-label="GPUs">GPUs</h2>
				<p aria-label="GPUs Description">
					Graphics card performance and specifications.
				</p>
				<span class="arrow">&rarr;</span>
			</a>
			<a
				href="/laptops-cpu-ranking"
				class="card"
				aria-label="Laptop Processor Ranking"
			>
				<h2 aria-label="Laptop CPUs">Laptop Processor Ranking</h2>
				<p aria-label="Laptop CPUs Ranking Description">
					Ranking of mobile processors by performance.
				</p>
				<span class="arrow">&rarr;</span>
			</a>
			<a
				href="/laptops-gpu-ranking"
				class="card"
				aria-label="Laptop GPU Ranking"
			>
				<h2 aria-label="Laptop GPUs">Laptop GPU Ranking</h2>
				<p aria-label="Laptop GPUs Ranking Description">
					Ranking of mobile graphics cards by performance.
				</p>
				<span class="arrow">&rarr;</span>
			</a>
			<a
				href={`${import.meta.env.BASE_URL}/compare`}
				class="card"
				aria-label="Component & Laptop Comparisons"
			>
				<h2 aria-label="Comparisons">Component & Laptop Comparisons</h2>
				<p aria-label="Comparisons Description">
					Compare CPUs, GPUs, and laptops side-by-side with detailed specs.
				</p>
				<span class="arrow">&rarr;</span>
			</a>
		</div>
	</section>
</Layout>

<style>
	.hero {
		padding: var(--space-6) 0;
	}

	.intro {
		font-size: var(--text-lg);
		color: var(--color-text-muted);
		max-width: 60ch;
	}

	.card {
		display: flex;
		flex-direction: column;
		padding: var(--space-4);
		border: var(--border-primary);
		text-decoration: none;
		color: var(--color-text);
		transition: transform 0.1s ease;
	}

	.card:hover {
		background-color: var(--color-surface);
		transform: translateY(-2px);
		box-shadow: 2px 2px 0 var(--color-border);
	}

	.card h2 {
		font-size: var(--text-2xl);
		margin-bottom: var(--space-2);
	}

	.card p {
		color: var(--color-text-muted);
		margin-bottom: var(--space-4);
		flex-grow: 1;
	}

	.arrow {
		align-self: flex-end;
		font-size: var(--text-xl);
	}
</style>
