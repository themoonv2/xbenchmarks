---
import Layout from "../../layouts/Layout.astro";
import gpus from "../../data/gpus.json";
import { getBrand, slugify } from "../../utils/data-helpers";

// Get unique brands
const brands = [...new Set(gpus.map((gpu) => getBrand(gpu.name)))].sort();
---

<Layout title="GPU Brands" description="Browse GPUs by brand">
    <section class="section">
        <h1>GPU Brands</h1>
        <p class="intro">Browse graphics cards by manufacturer</p>

        <div class="grid brand-grid">
            {
                brands.map((brand) => {
                    const brandGpus = gpus.filter(
                        (gpu) => getBrand(gpu.name) === brand,
                    );
                    return (
                        <a
                            href={`${import.meta.env.BASE_URL}/gpus/${slugify(brand)}`}
                            class="brand-card"
                        >
                            <h2>{brand}</h2>
                            <span class="count">
                                {brandGpus.length} graphics cards
                            </span>
                        </a>
                    );
                })
            }
        </div>
    </section>
</Layout>

<style>
    .intro {
        color: var(--color-text-muted);
        margin-bottom: var(--space-6);
    }

    .brand-grid {
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: var(--space-4);
    }

    .brand-card {
        padding: var(--space-6);
        border: var(--border-primary);
        text-decoration: none;
        color: var(--color-text);
        display: flex;
        flex-direction: column;
        gap: var(--space-2);
        transition: all 0.2s;
    }

    .brand-card:hover {
        background: var(--color-surface);
        transform: translateY(-2px);
    }

    .brand-card h2 {
        margin: 0;
        font-size: var(--text-xl);
    }

    .count {
        font-size: var(--text-sm);
        color: var(--color-text-muted);
    }
</style>
